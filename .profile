#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

# Add the path to jekyll's default installation directory to $PATH.
PATH=$PATH:/home/nathan/.gem/ruby/2.2.0/bin
PATH=/opt/anaconda/bin:$PATH
export PATH
