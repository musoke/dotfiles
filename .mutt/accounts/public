set realname    = "Nathan Musoke"
set from        = address@redacted"
set sendmail    = "/usr/bin/msmtp -a public"
# Where read mail in $spoolfile would be appended
set mbox        = "+Public/archive"
set postponed   = "+Public/drafts"
set trash       = "+Public/trash"
# Set where I store spam
set my_spam     = "+Public/spam"

color status cyan default

source ~/.mutt/accounts/account-settings


unalternates *
alternates \
    '^address@redacted$' \
    '^address@redacted$' \
    '^address@redacted$' \
    '^address@redacted$' \
    '^address@redacted$'
